apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: build
on:
  push:
    branches:
      - "**"
  workflow_dispatch:
jobs:
  test:
    outputs:
      CODE_COVERAGE: ${{ steps.RunUnitTest.outputs.CODE_COVERAGE }}
    steps:
      - uses: cloudbees-io/checkout@v1
        name: Get source code
      - name: Run unit tests
        id: RunUnitTest
        kind: test
        uses: docker://golang:1.23
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest
          swag init
          go test -coverprofile=coverage.out -v ./...
          go build
          cat coverage.out >> $CLOUDBEES_OUTPUTS/CODE_COVERAGE
      - name: Publish evidence
        uses: cloudbees-io/publish-evidence-item@v1
        with:
          content: |-
            ## Test code coverage
            ${{ steps.RunUnitTest.outputs.CODE_COVERAGE }}
          format: MARKDOWN
  build:
    uses: cb-golden-demo-org/sd-hackers-workflows/container-build.yaml
    vars: inherit
    secrets: inherit
    with:
      appName: hackers-api
      versionStart: "3.0"
      repo: cb-golden-demo-org/sd-hackers-api
    needs: test
